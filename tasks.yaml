includes:
  - cleanup: ./tasks/cleanup.yaml
  - create: ./tasks/create.yaml
  - deploy: https://raw.githubusercontent.com/defenseunicorns/uds-common-tasks/v0.3.6/tasks/deploy.yaml
  - setup: https://raw.githubusercontent.com/defenseunicorns/uds-common-tasks/v0.3.6/tasks/setup.yaml
  - test: ./tasks/test.yaml

tasks:
  - name: default
    description: Create K3D Cluster with UDS-Core-Istio and Sonarqube
    actions:
      - task: create-test-bundle
      - task: setup-cluster
      - task: deploy-test-bundle

  - name: create-package
    actions:
      - task: create:sonarqube-package

  - name: deploy-package
    actions:
      - task: deploy:package

  - name: cleanup
    actions:
      - task: cleanup:destroy

# CI will execute the following (via uds-common/.github/actions/test) so they need to be here with these names

  - name: create-test-bundle
    actions:
      - task: create:sonarqube-test-bundle

  - name: setup-cluster
    actions:
      - task: setup:k3d-test-cluster

  - name: deploy-test-bundle
    actions:
      - task: deploy:test-bundle

  - name: test-package
    actions:
      - task: test:health-check
      - task: test:ingress
