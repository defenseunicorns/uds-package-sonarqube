includes:
  - cleanup: ./tasks/cleanup.yaml
  - setup: https://raw.githubusercontent.com/defenseunicorns/uds-common-tasks/v0.1.0/tasks/setup.yaml
  - create: ./tasks/create.yaml
  - deploy: https://raw.githubusercontent.com/defenseunicorns/uds-common-tasks/v0.1.0/tasks/deploy.yaml
  - test: ./tasks/test.yaml

tasks:
  - name: setup-cluster
    actions:
      - task: setup:k3d-test-cluster
  
  - name: create-package
    actions:
      - task: create:sonarqube-package

  - name: create-test-bundle
    actions:
      - task: create:sonarqube-test-bundle
  
  - name: deploy-package
    actions:
      - task: deploy:package

  - name: deploy-test-bundle
    actions:
      - task: deploy:test-bundle

  - name: test-package
    actions:
      - task: test:health-check
      - task: test:ingress

  - name: cleanup
    actions:
      - task: cleanup:destroy
